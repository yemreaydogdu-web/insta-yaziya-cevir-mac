name: Build macOS app

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-macos:
    name: Build Zelka Scribe.app
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Build app and DMG
        run: bash build_macos.sh

      - name: Zip app
        run: |
          cd dist
          ditto -c -k --sequesterRsrc --keepParent "Zelka Scribe.app" ZelkaScribe.app.zip

      - name: Upload app artifact
        uses: actions/upload-artifact@v4
        with:
          name: ZelkaScribe-macOS-app
          path: dist/ZelkaScribe.app.zip

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: ZelkaScribe-macOS-dmg
          path: dist/ZelkaScribe.dmg
